{
  "name": "@techorbit/logger",
  "version": "0.0.0",
  "private": true,
  "type": "module",
  "exports": {
    ".": "./src/index.ts"
  },
  "scripts": {
    "build": "tsc --build",
    "lint": "eslint src --max-warnings 0",
    "typecheck": "tsc --noEmit",
    "test": "vitest run",
    "clean": "rm -rf dist .tsbuildinfo"
  },
  "dependencies": {
    "pino": "^8.21.0",
    "pino-http": "^9.0.0"
  },
  "devDependencies": {
    "@types/node": "^25.6.0",
    "@types/pino": "^7.0.0",
    "vitest": "^1.6.1"
  }
}